spec:
  containers:
    - name: snowsarva-frontend
      image: /snowsarva_image_database/snowsarva_image_schema/snowsarva_img_repo/snowsarva_frontend
      env:
        NODE_ENV: production
  endpoints:
    - name: app
      port: 80
      public: true